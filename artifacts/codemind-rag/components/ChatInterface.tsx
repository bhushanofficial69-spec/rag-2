"use client";

// Phase 8 implementation: Full chat interface with streaming answers
// Scaffold only — implementation coming in Phase 8

export interface ChatInterfaceProps {
  repoName?: string;
}

export default function ChatInterface({ repoName }: ChatInterfaceProps) {
  return (
    <div style={{ padding: "1rem" }}>
      <p style={{ color: "#64748b", fontSize: "0.875rem" }}>
        Chat interface — Phase 8 implementation
      </p>
    </div>
  );
}
